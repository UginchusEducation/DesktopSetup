DEFINE_LOG_CATEGORY_STATIC(LogBasePickup, All, All);
UE_LOG(LogBasePickup, Display, TEXT("Player %s is dead"), *GetName());

%s - string
%f - float

UE_LOG(LogRunMapSubsystem, Display, TEXT("Index Corrector = %i /// Same Index = %s"), IndexCorrector, (SameIndex ? TEXT("true") : TEXT("false")));
UE_LOG(LogTemp, Log, TEXT("Actor location: %s"), *ActorLocation.ToString());

---------------------
